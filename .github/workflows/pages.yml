name: deploy-audio-demo

on:
  push:
    branches: [main]
    paths:
      - "demos/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Prepare static demo
        run: |
          mkdir -p _site/audioseal _site/wavmark _site/timbrewm _site/voicemark _site/wmcodec
          cp demos/index.html _site/index.html
          cp demos/source_reference.wav demos/source_reference.mp3 _site/
          cp demos/audioseal/*.{wav,mp3} _site/audioseal/
          cp demos/wavmark/*.{wav,mp3} _site/wavmark/
          cp demos/timbrewm/*.{wav,mp3} _site/timbrewm/
          cp demos/voicemark/*.{wav,mp3} _site/voicemark/
          cp demos/wmcodec/*.{wav,mp3} _site/wmcodec/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
