# Numerical computing
numpy>=1.23.0
scipy>=1.10.0

# Audio I/O and processing
soundfile>=0.12.1
librosa>=0.10.0

# Deep learning (install a CUDA-compatible build when needed)
torch>=2.0.0
torchaudio>=2.0.0

# Watermark packages
audioseal>=0.2.0
wavmark>=0.0.3

# Audio-quality metrics
pesq>=0.0.4
pystoi>=0.3.3

# Configuration and tabular data
PyYAML>=6.0
pandas>=2.0.0
tqdm>=4.65.0

# TimbreWM dependencies
julius>=0.2.8
audiomentations>=0.28.0
kornia>=0.8.3

# VoiceMark and WMCodec dependencies
einops>=0.7.0
matplotlib>=3.7.0
